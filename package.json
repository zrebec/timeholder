{
  "name": "timetrack",
  "version": "1.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node tests/run.js",
    "archive": "npm run build && node scripts/archive-project.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "vite": "^8.0.13",
    "prettier": "^3.8.4",
    "zip-lib": "1.4.0"
  }
}
